{
  "name": "luterna",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "pnpm --stream -r run lint",
    "type-check": "pnpm --stream -r run type-check",
    "test": "pnpm --stream -r run test",
    "lint:ci": "pnpm lint && pnpm type-check"
  },
  "private": true,
  "devDependencies": {
    "@types/jsdom": "^20.0.1",
    "@types/node": "^18.11.12",
    "eslint": "^8.22.0",
    "jsdom": "^20.0.3",
    "prettier": "^2.7.1"
  },
  "resolutions": {
    "vue": "3.2.47"
  }
}
